services:
  - type: web
    name: contracthub-backend-v2
    env: python
    plan: free
    pythonVersion: 3.11.9
    buildCommand: pip install --upgrade pip && pip install -r requirements.txt
    startCommand: uvicorn app.main:app --host 0.0.0.0 --port $PORT
    envVars:
      - key: DATABASE_URL
        value: "postgresql://postgres:shreyasananya@db.iftmelhmlsngxdcuryuu.supabase.co:5432/postgres"
      - key: SECRET_KEY
        value: "super-secret-change-in-production"
      - key: JWT_SECRET
        value: "super-secret-change-in-production"
      - key: CORS_ORIGIN
        value: "https://saasdashboard-kappa.vercel.app"
      - key: EMBEDDING_DIM
        value: "8"

